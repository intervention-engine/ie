// Code generated by goagen v1.2.0-dirty, DO NOT EDIT.
//
// API "api": event TestHelpers
//
// Command:
// $ goagen
// --design=github.com/intervention-engine/ie/design
// --out=$(GOPATH)/src/github.com/intervention-engine/ie
// --version=v1.2.0-dirty

package test

import (
	"bytes"
	"context"
	"fmt"
	"github.com/goadesign/goa"
	"github.com/goadesign/goa/goatest"
	"github.com/intervention-engine/ie/app"
	"io"
	"log"
	"net/http"
	"net/http/httptest"
	"net/url"
	"time"
)

// ListEventBadRequest runs the method List of the given controller with the given parameters.
// It returns the response writer so it's possible to inspect the response headers and the media type struct written to the response.
// If ctx is nil then context.Background() is used.
// If service is nil then a default service is created.
func ListEventBadRequest(t goatest.TInterface, ctx context.Context, service *goa.Service, ctrl app.EventController, id string, endTime *time.Time, riskServiceID *string, startTime *time.Time, type_ *string) (http.ResponseWriter, error) {
	// Setup service
	var (
		logBuf bytes.Buffer
		resp   interface{}

		respSetter goatest.ResponseSetterFunc = func(r interface{}) { resp = r }
	)
	if service == nil {
		service = goatest.Service(&logBuf, respSetter)
	} else {
		logger := log.New(&logBuf, "", log.Ltime)
		service.WithLogger(goa.NewLogger(logger))
		newEncoder := func(io.Writer) goa.Encoder { return respSetter }
		service.Encoder = goa.NewHTTPEncoder() // Make sure the code ends up using this decoder
		service.Encoder.Register(newEncoder, "*/*")
	}

	// Setup request context
	rw := httptest.NewRecorder()
	query := url.Values{}
	if endTime != nil {
		sliceVal := []string{(*endTime).Format(time.RFC3339)}
		query["end_time"] = sliceVal
	}
	if riskServiceID != nil {
		sliceVal := []string{*riskServiceID}
		query["risk_service_id"] = sliceVal
	}
	if startTime != nil {
		sliceVal := []string{(*startTime).Format(time.RFC3339)}
		query["start_time"] = sliceVal
	}
	if type_ != nil {
		sliceVal := []string{*type_}
		query["type"] = sliceVal
	}
	u := &url.URL{
		Path:     fmt.Sprintf("/api/patients/%v/events", id),
		RawQuery: query.Encode(),
	}
	req, err := http.NewRequest("GET", u.String(), nil)
	if err != nil {
		panic("invalid test " + err.Error()) // bug
	}
	prms := url.Values{}
	prms["id"] = []string{fmt.Sprintf("%v", id)}
	if endTime != nil {
		sliceVal := []string{(*endTime).Format(time.RFC3339)}
		prms["end_time"] = sliceVal
	}
	if riskServiceID != nil {
		sliceVal := []string{*riskServiceID}
		prms["risk_service_id"] = sliceVal
	}
	if startTime != nil {
		sliceVal := []string{(*startTime).Format(time.RFC3339)}
		prms["start_time"] = sliceVal
	}
	if type_ != nil {
		sliceVal := []string{*type_}
		prms["type"] = sliceVal
	}
	if ctx == nil {
		ctx = context.Background()
	}
	goaCtx := goa.NewContext(goa.WithAction(ctx, "EventTest"), rw, req, prms)
	listCtx, _err := app.NewListEventContext(goaCtx, req, service)
	if _err != nil {
		panic("invalid test data " + _err.Error()) // bug
	}

	// Perform action
	_err = ctrl.List(listCtx)

	// Validate response
	if _err != nil {
		t.Fatalf("controller returned %+v, logs:\n%s", _err, logBuf.String())
	}
	if rw.Code != 400 {
		t.Errorf("invalid response status code: got %+v, expected 400", rw.Code)
	}
	var mt error
	if resp != nil {
		var ok bool
		mt, ok = resp.(error)
		if !ok {
			t.Fatalf("invalid response media: got %+v, expected instance of error", resp)
		}
	}

	// Return results
	return rw, mt
}

// ListEventInternalServerError runs the method List of the given controller with the given parameters.
// It returns the response writer so it's possible to inspect the response headers and the media type struct written to the response.
// If ctx is nil then context.Background() is used.
// If service is nil then a default service is created.
func ListEventInternalServerError(t goatest.TInterface, ctx context.Context, service *goa.Service, ctrl app.EventController, id string, endTime *time.Time, riskServiceID *string, startTime *time.Time, type_ *string) (http.ResponseWriter, error) {
	// Setup service
	var (
		logBuf bytes.Buffer
		resp   interface{}

		respSetter goatest.ResponseSetterFunc = func(r interface{}) { resp = r }
	)
	if service == nil {
		service = goatest.Service(&logBuf, respSetter)
	} else {
		logger := log.New(&logBuf, "", log.Ltime)
		service.WithLogger(goa.NewLogger(logger))
		newEncoder := func(io.Writer) goa.Encoder { return respSetter }
		service.Encoder = goa.NewHTTPEncoder() // Make sure the code ends up using this decoder
		service.Encoder.Register(newEncoder, "*/*")
	}

	// Setup request context
	rw := httptest.NewRecorder()
	query := url.Values{}
	if endTime != nil {
		sliceVal := []string{(*endTime).Format(time.RFC3339)}
		query["end_time"] = sliceVal
	}
	if riskServiceID != nil {
		sliceVal := []string{*riskServiceID}
		query["risk_service_id"] = sliceVal
	}
	if startTime != nil {
		sliceVal := []string{(*startTime).Format(time.RFC3339)}
		query["start_time"] = sliceVal
	}
	if type_ != nil {
		sliceVal := []string{*type_}
		query["type"] = sliceVal
	}
	u := &url.URL{
		Path:     fmt.Sprintf("/api/patients/%v/events", id),
		RawQuery: query.Encode(),
	}
	req, err := http.NewRequest("GET", u.String(), nil)
	if err != nil {
		panic("invalid test " + err.Error()) // bug
	}
	prms := url.Values{}
	prms["id"] = []string{fmt.Sprintf("%v", id)}
	if endTime != nil {
		sliceVal := []string{(*endTime).Format(time.RFC3339)}
		prms["end_time"] = sliceVal
	}
	if riskServiceID != nil {
		sliceVal := []string{*riskServiceID}
		prms["risk_service_id"] = sliceVal
	}
	if startTime != nil {
		sliceVal := []string{(*startTime).Format(time.RFC3339)}
		prms["start_time"] = sliceVal
	}
	if type_ != nil {
		sliceVal := []string{*type_}
		prms["type"] = sliceVal
	}
	if ctx == nil {
		ctx = context.Background()
	}
	goaCtx := goa.NewContext(goa.WithAction(ctx, "EventTest"), rw, req, prms)
	listCtx, _err := app.NewListEventContext(goaCtx, req, service)
	if _err != nil {
		panic("invalid test data " + _err.Error()) // bug
	}

	// Perform action
	_err = ctrl.List(listCtx)

	// Validate response
	if _err != nil {
		t.Fatalf("controller returned %+v, logs:\n%s", _err, logBuf.String())
	}
	if rw.Code != 500 {
		t.Errorf("invalid response status code: got %+v, expected 500", rw.Code)
	}
	var mt error
	if resp != nil {
		var ok bool
		mt, ok = resp.(error)
		if !ok {
			t.Fatalf("invalid response media: got %+v, expected instance of error", resp)
		}
	}

	// Return results
	return rw, mt
}

// ListEventNotFound runs the method List of the given controller with the given parameters.
// It returns the response writer so it's possible to inspect the response headers and the media type struct written to the response.
// If ctx is nil then context.Background() is used.
// If service is nil then a default service is created.
func ListEventNotFound(t goatest.TInterface, ctx context.Context, service *goa.Service, ctrl app.EventController, id string, endTime *time.Time, riskServiceID *string, startTime *time.Time, type_ *string) (http.ResponseWriter, error) {
	// Setup service
	var (
		logBuf bytes.Buffer
		resp   interface{}

		respSetter goatest.ResponseSetterFunc = func(r interface{}) { resp = r }
	)
	if service == nil {
		service = goatest.Service(&logBuf, respSetter)
	} else {
		logger := log.New(&logBuf, "", log.Ltime)
		service.WithLogger(goa.NewLogger(logger))
		newEncoder := func(io.Writer) goa.Encoder { return respSetter }
		service.Encoder = goa.NewHTTPEncoder() // Make sure the code ends up using this decoder
		service.Encoder.Register(newEncoder, "*/*")
	}

	// Setup request context
	rw := httptest.NewRecorder()
	query := url.Values{}
	if endTime != nil {
		sliceVal := []string{(*endTime).Format(time.RFC3339)}
		query["end_time"] = sliceVal
	}
	if riskServiceID != nil {
		sliceVal := []string{*riskServiceID}
		query["risk_service_id"] = sliceVal
	}
	if startTime != nil {
		sliceVal := []string{(*startTime).Format(time.RFC3339)}
		query["start_time"] = sliceVal
	}
	if type_ != nil {
		sliceVal := []string{*type_}
		query["type"] = sliceVal
	}
	u := &url.URL{
		Path:     fmt.Sprintf("/api/patients/%v/events", id),
		RawQuery: query.Encode(),
	}
	req, err := http.NewRequest("GET", u.String(), nil)
	if err != nil {
		panic("invalid test " + err.Error()) // bug
	}
	prms := url.Values{}
	prms["id"] = []string{fmt.Sprintf("%v", id)}
	if endTime != nil {
		sliceVal := []string{(*endTime).Format(time.RFC3339)}
		prms["end_time"] = sliceVal
	}
	if riskServiceID != nil {
		sliceVal := []string{*riskServiceID}
		prms["risk_service_id"] = sliceVal
	}
	if startTime != nil {
		sliceVal := []string{(*startTime).Format(time.RFC3339)}
		prms["start_time"] = sliceVal
	}
	if type_ != nil {
		sliceVal := []string{*type_}
		prms["type"] = sliceVal
	}
	if ctx == nil {
		ctx = context.Background()
	}
	goaCtx := goa.NewContext(goa.WithAction(ctx, "EventTest"), rw, req, prms)
	listCtx, _err := app.NewListEventContext(goaCtx, req, service)
	if _err != nil {
		panic("invalid test data " + _err.Error()) // bug
	}

	// Perform action
	_err = ctrl.List(listCtx)

	// Validate response
	if _err != nil {
		t.Fatalf("controller returned %+v, logs:\n%s", _err, logBuf.String())
	}
	if rw.Code != 404 {
		t.Errorf("invalid response status code: got %+v, expected 404", rw.Code)
	}
	var mt error
	if resp != nil {
		var ok bool
		mt, ok = resp.(error)
		if !ok {
			t.Fatalf("invalid response media: got %+v, expected instance of error", resp)
		}
	}

	// Return results
	return rw, mt
}

// ListEventOK runs the method List of the given controller with the given parameters.
// It returns the response writer so it's possible to inspect the response headers and the media type struct written to the response.
// If ctx is nil then context.Background() is used.
// If service is nil then a default service is created.
func ListEventOK(t goatest.TInterface, ctx context.Context, service *goa.Service, ctrl app.EventController, id string, endTime *time.Time, riskServiceID *string, startTime *time.Time, type_ *string) (http.ResponseWriter, app.EventCollection) {
	// Setup service
	var (
		logBuf bytes.Buffer
		resp   interface{}

		respSetter goatest.ResponseSetterFunc = func(r interface{}) { resp = r }
	)
	if service == nil {
		service = goatest.Service(&logBuf, respSetter)
	} else {
		logger := log.New(&logBuf, "", log.Ltime)
		service.WithLogger(goa.NewLogger(logger))
		newEncoder := func(io.Writer) goa.Encoder { return respSetter }
		service.Encoder = goa.NewHTTPEncoder() // Make sure the code ends up using this decoder
		service.Encoder.Register(newEncoder, "*/*")
	}

	// Setup request context
	rw := httptest.NewRecorder()
	query := url.Values{}
	if endTime != nil {
		sliceVal := []string{(*endTime).Format(time.RFC3339)}
		query["end_time"] = sliceVal
	}
	if riskServiceID != nil {
		sliceVal := []string{*riskServiceID}
		query["risk_service_id"] = sliceVal
	}
	if startTime != nil {
		sliceVal := []string{(*startTime).Format(time.RFC3339)}
		query["start_time"] = sliceVal
	}
	if type_ != nil {
		sliceVal := []string{*type_}
		query["type"] = sliceVal
	}
	u := &url.URL{
		Path:     fmt.Sprintf("/api/patients/%v/events", id),
		RawQuery: query.Encode(),
	}
	req, err := http.NewRequest("GET", u.String(), nil)
	if err != nil {
		panic("invalid test " + err.Error()) // bug
	}
	prms := url.Values{}
	prms["id"] = []string{fmt.Sprintf("%v", id)}
	if endTime != nil {
		sliceVal := []string{(*endTime).Format(time.RFC3339)}
		prms["end_time"] = sliceVal
	}
	if riskServiceID != nil {
		sliceVal := []string{*riskServiceID}
		prms["risk_service_id"] = sliceVal
	}
	if startTime != nil {
		sliceVal := []string{(*startTime).Format(time.RFC3339)}
		prms["start_time"] = sliceVal
	}
	if type_ != nil {
		sliceVal := []string{*type_}
		prms["type"] = sliceVal
	}
	if ctx == nil {
		ctx = context.Background()
	}
	goaCtx := goa.NewContext(goa.WithAction(ctx, "EventTest"), rw, req, prms)
	listCtx, _err := app.NewListEventContext(goaCtx, req, service)
	if _err != nil {
		panic("invalid test data " + _err.Error()) // bug
	}

	// Perform action
	_err = ctrl.List(listCtx)

	// Validate response
	if _err != nil {
		t.Fatalf("controller returned %+v, logs:\n%s", _err, logBuf.String())
	}
	if rw.Code != 200 {
		t.Errorf("invalid response status code: got %+v, expected 200", rw.Code)
	}
	var mt app.EventCollection
	if resp != nil {
		var ok bool
		mt, ok = resp.(app.EventCollection)
		if !ok {
			t.Fatalf("invalid response media: got %+v, expected instance of app.EventCollection", resp)
		}
	}

	// Return results
	return rw, mt
}
